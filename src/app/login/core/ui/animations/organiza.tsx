import Lottie from "lottie-react";
import animated from "@/assets/animated/media-scheduling.json";

const Organiza = () => {
	return <Lottie animationData={animated} loop={true} />;
};

export default Organiza;
